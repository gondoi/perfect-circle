# circle.yml
machine:
  services:
    - docker

test:
  pre:
    - |
      [[ -n "${CIRCLE_BRANCH}" ]] && IMAGE_TAG="latest" || IMAGE_TAG="${CIRCLE_TAG}"
  post:
    - echo ${IMAGE_TAG}


deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - echo $CIRCLE_BRANCH
