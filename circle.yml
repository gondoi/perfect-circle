# circle.yml
machine:
  services:
    - docker

test:
  post:
    - echo ${CIRCLE_BRANCH}${CIRCLE_TAG}


deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - echo $CIRCLE_BRANCH
